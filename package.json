{
  "name": "nopal",
  "private": true,
  "type": "module",
  "description": "Nopal continuous enforcement adapter for the pinned Pi distribution",
  "scripts": {
    "test": "node --test \"extensions/**/*.test.ts\""
  },
  "pi": {
    "extensions": [
      "extensions/policy-gate"
    ]
  }
}
